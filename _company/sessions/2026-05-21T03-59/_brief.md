# 📋 작업 브리프

**원 명령:** [자율 사이클 — 2026-05-21] 1인 기업 24시간 운영 중. 회사 목표·각 에이전트의 개인 목표(_agents/{id}/goal.md)·최근 의사결정·메모리를 검토해서 지금 가장 가치 있는 단일 작업 1개를 결정하고, 적절한 1~2명 에이전트에게 분배해서 실행하세요. 같은 산출물을 반복하지 마세요 — 메모리에 비슷한 항목이 24시간 내에 있으면 다른 각도로 진전시키세요.

## 요약
현재 시스템의 가장 중요한 병목 지점은 '핵심 로직을 실제로 동작하는 환경'으로 옮기는 것입니다. 따라서, 다음 단일 작업으로는 코다리에게 핵심 API 엔드포인트(`POST /api/v1/avoided_loss`)를 구축하고, 그 주변의 데이터 유효성 검사 및 오류 처리 구조(E2E 테스트 스켈레톤)를 최우선으로 구현하도록 지시합니다.

## 분배
- **💻 코다리**: 핵심 로직인 'Avoided Loss' API 엔드포인트(`POST /api/v1/avoided_loss`)의 뼈대 코드를 구축하십시오. Python/FastAPI를 사용하여 백엔드 구조를 완성하고, 반드시 다음 세 가지 기능을 포함해야 합니다: 1) 감정적 손실 변수 및 행동경제학 가중치를 계산하는 로직 골격. 2) 데이터 누락(Missing Field) 또는 상충 관계(Conflicting Data Type) 발생 시, 오류 코드와 함께 구조화된 피드백 객체(Structured Error JSON)를 반환하는 예외 처리 메커니즘. 3) 이 기능을 검증할 수 있는 최소한의 E2E 스트레스 테스트 골격 코드를 포함하여 주십시오.
